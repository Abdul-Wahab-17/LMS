<h1>Welcome <%= user || 'Guest' %></h1>

<ul>
    <% if (Array.isArray(items) && items.length) { %>
        <% items.forEach(item => { %>
            <li><%= item %></li>
        <% }) %>
    <% } else { %>
        <li>No items found</li>
    <% } %>
</ul>
